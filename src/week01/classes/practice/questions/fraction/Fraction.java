package week01.classes.practice.questions.fraction;

/**
 * Create a Fraction class with numerator and denominator.
 * Add methods to:
 * Simplify the fraction (e.g., 2/4 → 1/2).
 * Add two fractions (a/b + c/d = (ad + bc)/bd).
 */

public class Fraction {
}
